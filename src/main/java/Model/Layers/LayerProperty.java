package Model.Layers;

/**
 * Enum to describe which parameters a Layer has.
 */
public enum LayerProperty {
    OUTPUT_DIMENSION, INPUT_DIMENSION, ACTIVATION_FUNCTION, WINDOWSIZE, WINDOWSIZE2D, DROPRATE,
}

import Model.Graph.Graph;

public class ConformityChecker {

    /**
     * Checks if the graph is circular
     * NodeVisitor of visitorpattern
     * @param graph Graph to be checked
     * @return whether the graph has a circular structure
     */
    public boolean hasCircle(Graph graph) {

        return true;
    }

    /**
     * Checks if the graph has an Input and an Output and if every Node is connected and if there is a path
     * from Input to Output
     * @param graph Graph to be checked
     * @return whether the graph has a correct structure
     */
    public boolean hasCompletedStructure(Graph graph) {

        return true;
    }
}

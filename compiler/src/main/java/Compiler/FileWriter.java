package Compiler;

import java.net.URI;
import java.util.ArrayList;

public class FileWriter {

    private ArrayList<String> lines;

    /**
     * Adds a line to the text which is to be written
     * @param line
     */
    public void append(String line) {

    }

    /**
     * Writes the built String to a pythonFile in given Direction
     * @param destination directory to save the file in
     * @param ending ending of the file e.g. xml or py
     * @param fileName name of the file
     */
    public void writeToFile(URI destination, String fileName, String ending) {

    }
}

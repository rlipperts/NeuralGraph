package Layers;

/**
 * Enum to describe which parameters a Layer has.
 */
public enum LayerProperty {
    INPUT_DIMENSION, OUTPUT_DIMENSION, ACTIVATION_FUNCTION, WINDOWSIZE, WINDOWSIZE2D, POOLSIZE, POOLSIZE2D, DROPRATE,
}
